# Sprint 5 - Individual AI Workflows

Each team member adds a section: first prompt, agent output, kept/cut, next time.

---

### Cursor agent (initial scaffold)

**First prompt:** Read sprint5.md and build the Bug Tracker FE against the OpenAPI spec.

**Kept:** Next.js form posting to POST /v1/issues, env-based API URL, success/validation/network UI.

**Cut:** Auth and triage (out of scope for Sprint 5).