# Copy this file to .env.local and fill in the values.
# For local dev, point at your local BE (e.g. http://localhost:3001).
# For production (Vercel / Render), set this variable in the dashboard.
NEXT_PUBLIC_API_URL=https://tcss460-team-6-api.onrender.com
