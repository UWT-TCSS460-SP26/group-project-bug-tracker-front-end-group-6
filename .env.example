# Copy this file to .env.local and fill in the values.
# This file is safe to commit — it contains no secrets.

# Your deployed API base URL (no trailing slash).
# In production, point this to your Render/Railway/etc. URL.
# In development, use http://localhost:3000 (or whatever port your BE runs on).
NEXT_PUBLIC_API_URL=https://tcss460-team-6-api.onrender.com
